services:
  auth:
    image: auth
    build:
      context: services
      dockerfile: auth.Dockerfile
    env_file:
      - ./services/apis/auth/.env.example
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
