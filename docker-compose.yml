services:
  auth:
    image: auth
    build:
      context: services
      dockerfile: auth.Dockerfile
    env_file:
      - ./services/apis/auth/.env
    environment:
      NODE_ENV: production
    ports:
      - 4000:4000
