/**
 * @swagger
 * components:
 *   parameters:
 *     TicketStatusQuery:
 *       name: status
 *       in: query
 *       description: Filter by ticket status
 *       required: false
 *       schema:
 *         $ref: '#/components/schemas/TicketStatus'
 */
